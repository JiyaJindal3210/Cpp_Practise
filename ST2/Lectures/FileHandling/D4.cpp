#include <iostream>
using namespace std;

int main() {
    // we have 2 types of pointers for reading and writing in c++ file handling:
    // 1. seekp/seekg - modify the position of reading and writing pointer
    // 2. tellp/tellg - get the position of reading and writing pointer

    // Note : p means put (writing) , g means get(reading)
}